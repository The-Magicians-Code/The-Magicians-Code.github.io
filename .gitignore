# OS
**/.DS_Store

# Env
.env
.env.production
.env.development
.env.local
.env.*.local

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Astro
dist/
.astro/

# Build-time generated resume PDF (dev convenience copy; CI writes it into dist/)
public/Tanel_Treuberg_Software_Engineer.pdf

# IDE / editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# Logs
logs/
*.log

# Local screenshot scratchpad (e.g. Chrome DevTools MCP captures)
.scratch/

# Brainstorming visual companion scratch
.superpowers/

# impeccable live-mode local config (per-machine dev tooling: HTML entries,
# CSP-checked flag, dev-server paths). The canonical design spec
# (DESIGN.md and .impeccable/design.json) IS committed — the live/
# subdirectory is the only impeccable artifact that's local-only.
.impeccable/live/
